ignore:
    - "include/external"
    - "programs"
    - "single_include"
    - "single_include_test"
    - "tests"
coverage:
    status:
        project:
            default:
                target: auto
                informational: true
        patch:
            default:
                target: auto
                informational: true